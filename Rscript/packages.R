require(here)
#require(pdftools)
require(stringr)
library(XML)
library(httr)
require(dplyr)
require(magick)# animation

library (raster) # spatial data
require(sp)
require(rgeos)
require(rgdal)
require(dplyr)
require(CoordinateCleaner)

# maps
require(rnaturalearth)
require(ggplot2)
require(maps)
require(sna)

library(rasterVis)
library(viridis)
require(gridExtra)
library(ggrepel)
require(phylin)
